{
  "name": "promptflow-extension",
  "version": "0.1.0",
  "description": "Browser-level Prompt Command System",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "npm run build:ts && npm run copy:assets && npm run generate-icons && npm run cleanup",
    "build:ts": "tsc",
    "copy:assets": "cp manifest.json dist/ && mkdir -p dist/icons && cp src/panel.css dist/",
    "generate-icons": "node generate-icons.js",
    "cleanup": "rm -rf dist/types dist/utils",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "canvas": "^3.2.3",
    "typescript": "^5.4.0"
  }
}
